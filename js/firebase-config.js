/* ===================================================================
   FIREBASE CONFIG  ——  paste your project's config here.
   Full click-by-click guide is in  FIREBASE-SETUP.md
   (You only ever edit the values inside the { } below.)
   =================================================================== */
window.FIREBASE_CONFIG = {
  apiKey: "",
  authDomain: "",
  projectId: "",
  storageBucket: "",
  messagingSenderId: "",
  appId: ""
};

/* Owner access (the email must match the Email/Password user you create
   in Firebase Auth during setup — its password is stored safely in Firebase,
   NOT here). The secret code is the extra gate after the password. */
window.OWNER_EMAIL = "richardrich9888@gmail.com";
window.OWNER_SECRET_CODE = "tayyabisdady";
